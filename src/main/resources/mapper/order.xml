<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mall.mapper.OrderMapper">
    <insert id="save">
        INSERT INTO orders(order_id, customer_id, product_id, quantity, order_date, status)
        VALUES (#{order.orderId}, #{order.customerId}, #{order.productId},
        #{order.quantity}, #{order.orderDate}, #{order.status});
    </insert>

    <select id="getOrders">
        SELECT * from orders;
    </select>

    <select id="getOrderById" parameterType="int" resultType="com.example.mall.dto.OrderDto">
        SELECT * from orders where order_id = #{orderId};
    </select>
</mapper>
